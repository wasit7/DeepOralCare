import { defineStore } from "pinia";

export const useRootStore = defineStore("root", {
  state: () => ({
    // state
    user: null,
  }),
  getters: {
    // getters
  },
  actions: {
    // actions
  },
});
